version: '3'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    container_name: backend

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    container_name: frontend